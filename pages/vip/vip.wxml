<view wx:for="{{list}}" wx:key="list">
 <view>{{item.id}}</view>
 <view>{{item.title}}</view>
</view>

<view wx:for="{{list}}" wx:key="list" class="cu-capsule">
    <view class="cu-tag bg-red"><text class="cuIcon-likefill"></text></view>
    <view class="cu-tag line-red">
      {{item.title}}
    </view>
  </view>

  <view wx:for="{{list}}" wx:key="list"  class="padding bg-white">
  <view class="text-cut padding bg-grey radius" style="width:100%">  {{item.title}}</view>
  </view>